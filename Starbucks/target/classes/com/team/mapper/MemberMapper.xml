<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.mapper.MemberMapper">
	<insert id="postJoin">
		insert into member
		values
		(#{userId},#{userPw},#{userName},#{userGender},
		#{userBirthYear},#{userBirthMonth},#{userBirthDay},
		#{userBirthFlag},#{userPhone},#{userEmail})
	</insert>

	<select id="postLogin" resultType="com.team.domain.MemberDTO">
		select *
		from member
		where userId=#{userId}
		and userPw=#{userPw}
	</select>
	
	<select id="checkId" parameterType="String" resultType="int">
		SELECT COUNT(userId) 
		FROM member 
		WHERE userId=#{userId};
	</select>
</mapper>